if(ANDROID)
elseif(WIN32)
  set(pTS_SUPPORT_SPECIFIC_HDR)
  set(pTS_SUPPORT_SPECIFIC_SRC)
elseif(APPLE)
  if(MACOSX)
    set(pTS_SUPPORT_SPECIFIC_HDR)
    set(pTS_SUPPORT_SPECIFIC_SRC)
  elseif(IOS)
    set(pTS_SUPPORT_SPECIFIC_HDR)
    set(pTS_SUPPORT_SPECIFIC_SRC)
  endif()
elseif(LINUX)
  set(pTS_SUPPORT_SPECIFIC_HDR)
  set(pTS_SUPPORT_SPECIFIC_SRC)
endif()


set(pTS_SUPPORT_HDR
  ${pTS_SUPPORT_SPECIFIC_HDR}
  support/pTSMacros.h
  support/pTSLazyMacros.h
)

set(pTS_SUPPORT_SRC
  ${pTS_SUPPORT_SPECIFIC_SRC}
)
